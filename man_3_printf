.Dd Nov 04 2021
.Dt _printf(3)
.Nm _printf
.Sh SYNOPSIS
_printf(const char *format, ...)

.Sh DESCRIPTION
 _printf() produces output according to a format.
	 Format string is a character string passed to function. It can be
	 composed of zero or more directives called conversion specifiers, '%' followed by a specifier.
.Sh PROTOTYPE

.Sh TYPES OF CONVERSION  
The '%' character and an indicated conversion type.
.It i
argument is converted to int.
.It d
argument is converted to int.
.It c
argument is converted into a character.
.It s
argument is converted into a string.
.It %
'%%' argument is converted into.

.SH AUTHORS
Adam Brummer & Larry Nevins